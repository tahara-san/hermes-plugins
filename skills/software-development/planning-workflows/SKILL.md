---
name: planning-workflows
description: "Use when planning, documenting, executing, cleaning, or converting implementation plans and task directories across Hermes/Claude-style workflows."
version: 1.0.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [planning, implementation-plans, plan-mode, tasks, execution, cleanup]
    related_skills: [subagent-driven-development, requesting-code-review, test-driven-development]
---

# Planning Workflows

## Overview

This umbrella skill covers the planning class: writing implementation plans, operating in plan-only mode, creating structured task docs, executing existing task plans, converting out-of-scope issue logs, and cleaning completed task directories.

Migrated detailed references are under `references/` with source prefixes. Use `references/plan-code-delta-review-rerounds.md` when an explicit `plan-code` review gate has a prior clean full-review baseline and post-fix rerounds can safely review only changed, semantically affected, or no-baseline content while carrying unchanged clean verdicts forward. Use `references/out-of-scope-issue-cleanup-audit.md` when the user asks to clean or triage `tasks/out-of-scope-issues/` without immediate removals: enumerate every issue, verify against current source or external source of truth, classify fixed/stale/partial/active, and report cleanup candidates only. Use `references/plan-doc-issue-conversion-currently-satisfied.md` when converting a single out-of-scope issue into a plan-doc and current source/tests already appear to satisfy it: create verification-first task docs, preserve historical issue traceability, remove the issue only after docs exist, keep duplicate kickoff prompts synchronized, and use final artifact-consistency after review artifact/TODO edits. Use `references/plan-code-multi-task-batch-and-no-change-closures.md` when the user invokes `plan-code` over a directory containing multiple task dirs: triage implementable/no-change/blocked tasks first, close already-satisfied tasks with source/test snapshots instead of churn, and save pending Codex-review blocker artifacts when a mandatory delegate verdict has not returned. Use `references/plan-issues-priority-grouped-conversion.md` when the user invokes `/skill plan-issues <priority>` or asks to convert priority-filtered out-of-scope issues into grouped task dirs: read all issue files including `manual/`, ask an early scope decision when grouping is ambiguous, group by class/ownership, preserve source issue files, and mechanically verify every issue maps to exactly one generated plan. Use `references/plan-doc-clarification-defaults.md` when a plan-doc interview times out or the agent must choose safe defaults and document them. Use `references/plan-doc-fast-path-and-bounded-reviews.md` when an explicit plan-doc task is getting slow due to over-inspection or roaming external reviewers; create the plan bundle early, then run bounded reviews against that concrete bundle. Use `references/scoped-plan-code-review-gates.md` when the user asks for a plan-code-style simplify/review gate on an ad-hoc implementation diff without a `tasks/<slug>/` plan; run the quality gates and save scoped artifacts without pretending a full task-directory `/plan-code` flow completed. Use `references/buffdemy2-web-profile-feed-contract-gates.md` when executing Buffdemy2-web profile feed work backed by Question / QuestionAnswer APIs: prove owner-list, cursor, and schema contracts through the live frontend proxy before final UI/review, and stop fail-closed when the backend only supports per-parent thread queries. Use `references/buffdemy2-web-tag-feed-plan-doc.md` when planning `/tags/content` tag feed changes such as adding a Question tab or hiding Refresh: account for SSR initial fetches not being Playwright-route-observable after Refresh removal, require tag-query contract gates, verify nested full-post selectors, and spell out tab-state, cursor-type, and i18n-key details. Use `references/buffdemy2-web-tag-feed-plan-code.md` when executing those plans: prove query contracts by layer, remove reachable Refresh UI honestly, hoist Radix Tabs to the shared client boundary, consider force-mounted tab panels for feed state, extract shared Question feed renderers outside profile namespaces, and handle reviewer non-blocking verification gaps without staling approvals unnecessarily. Use `references/plan-code-phase0-backend-blocker-stop.md` when a `/skill plan-code` task has unresolved Phase 0 backend/API gates: verify current source, update TODO/notes/backend instructions with file-level blocker evidence, and stop before frontend/client edits. Use `references/buffdemy-backend-stale-doc-authority.md` when Buffdemy backend task docs, reviewers, or tests cite `docs/` files as API/implementation contract evidence; by default those docs are stale/transient context unless the user explicitly elevates them. Use `references/plan-code-backend-gate-reverification.md` when an explicit `plan-code` task is blocked on an upstream backend/API contract and you must re-verify the gate from current route/schema/source before making frontend/client edits. Use `references/backend-gate-fail-closed-ui.md` when a frontend task depends on an unmet backend/API companion: keep the live UI fail-closed instead of enabling known-bad requests, document future behavior as backend-blocked, and rerun review after stale task-doc fixes. Use `references/plan-code-large-diff-budget-and-checkpoints.md` when a large `plan-code` diff is cycling through post-review fixes and you need micro-bundles, budget checkpoints, and fail-closed handoff rules. Use `references/capability-boundary-plan-code-pattern.md` when executing API/auth/cache capability-boundary plans that must prove who can read, fetch, scrape, or write. Use `references/plan-skill-invocation-and-sharing.md` when explaining why `plan-doc` / `plan-code` may be loadable even if archived or absent from `hermes skills list`, or when preparing the planning-family skills for publishing. Use `references/buffdemy-backend-plan-doc-review-verification.md` for Buffdemy backend plan-doc verification-command and lifecycle/error-code review pitfalls. Use `references/buffdemy-backend-search-projection-plan-doc.md` when planning Elasticsearch/search projection rewrites from Mongo source models, especially pre-service no-compat migrations and parent/child-content projection lifecycles. Use `references/buffdemy-backend-search-endpoint-inventory.md` when inventorying which Buffdemy V1 search services are exposed as API endpoints versus only present as collection filters, Elasticsearch models, or unifiedConsumer projection handlers. Use `references/buffdemy-backend-question-side-effect-plan-doc.md` when planning or reviewing Buffdemy backend Question creation side-effect parity bundles across metadata, mentions, search, post-tags, Neo4j, and outbox test matrices. Use `references/buffdemy-backend-question-optional-content-plan-doc.md` when planning Question title-only/optional body-or-papers changes: preserve raw paper intent before validation, avoid partial-update default clearing, keep `papers.length === 0 || hasReadyPaperContent(papers)`, and rerun stale plan-doc reviews after doc fixes. Use `references/buffdemy-backend-question-author-content-required-plan-doc.md` for the reverse class at planning time: removing caller Question `title`, restoring required author content/body, or adding rendered-text minimums; scope the minimum to author writes instead of globally tightening shared ready/read/derived schemas, define code-point vs code-unit counting, and keep persisted/search title derived unless scope expands. Use `references/buffdemy-backend-question-author-content-plan-code.md` when executing that class of task: it captures normalized rendered-text/code-point enforcement, title derivation, author-only ready-row boundaries, downstream fixture/service verification, large-bundle review recovery, and final artifact-consistency checks. Use `references/buffdemy-backend-question-title-limit-plan-doc.md` when planning Buffdemy backend Question title-length/schema-limit changes: trace parent/content/API schemas plus outbox/search/unifiedConsumer/cronWorker/hydration side effects, keep `question.published` payload stable unless explicitly changed, include no-side-effect rejection tests, and add cronWorker verification whenever cron fixtures are in scope. Use `references/buffdemy-backend-stale-persisted-schema-plan-code.md` when executing a Buffdemy backend plan-code fix where a live route fails while hydrating a target document because persisted rows contain stale fields rejected by strict current schemas; prefer RED route+schema regressions, narrow hydration normalization, and an affected-service restart before further frontend edits. Use `references/buffdemy-question-activity-notifications-plan.md` when planning or reviewing Buffdemy Question / QuestionAnswer / QuestionFollowUpComment notification additions; it captures outbox fanout, ACL-safe recipients, async follow-up ordering, dedup/idempotency, raw deadline payloads, and frontend summary contracts. Use `references/buffdemy-question-score-reward-model-plan.md` when planning Question / QuestionAnswer rank/score, reward/Buff, reaction, list-save, wallet/ledger, or cronWorker reconciliation changes; it captures current backend/frontend contract gaps, event-driven stat propagation boundaries, paid-vs-free reward separation, and reconciliation pitfalls. Use `references/buffdemy-backend-multilingual-es-search-model.md` when planning or reviewing Buffdemy backend Article Elasticsearch indexing/search migration after the Article + ArticleContent split; it captures the preferred per-language search-document model, analyzer/alias/versioning gates, and fanout pitfalls. Use `references/buffdemy-backend-search-endpoint-readiness-plan-doc.md` when planning Buffdemy ES-backed API search endpoints: classify readiness across ES model, search queue/schema, outboxProcessor fanout, unifiedConsumer handlers, API route/hydration, and backfill needs; also captures the resource-local `/resource/search` route preference and `/user`-is-not-search pitfall. Use `references/buffdemy-backend-public-user-shape-contract.md` when plan-code work decides whether a Buffdemy user field belongs in the public user shape: trace `/user` and `/user/search` routes to `userRepository.toShape('public')`, update centralized `PublicUserData`/serializer tests, keep frontend proxies generic, and build scoped multi-repo review bundles amid unrelated dirty work. Use `references/buffdemy-backend-acl-write-conflict-retry-evaluation.md` when planning or auditing Buffdemy backend ACL/fixture `WriteConflict` retry work: map stale frontend labels to current backend routes first, distinguish production article transaction flow from test-only fixture reset/setup flows, check whether `AppError` wrapping defeats Mongo retry labels, and prefer boundary cleanup or narrow idempotent retries over broad handler loops. Use `references/plan-code-search-endpoint-review-failures.md` during plan-code execution/review for ES-backed search endpoints, especially to catch null-response fail-open behavior, query-scope drift, full-replacement producer gaps, and missing untracked files in final bundles. Use `references/buffdemy-search-projection-plan-code-lifecycle.md` when executing or reviewing Buffdemy backend ES projection lifecycle work that spans Mongo source rows, RabbitMQ search jobs, outboxProcessor fanout, unifiedConsumer handlers, versioned deletes, and reset/backfill reconciliation gates. Use `references/pre-service-no-compat-plan-code-cleanup.md` when the user clarifies mid-implementation that the feature is pre-service and DB/index reset is acceptable: remove stale-shape compatibility at every boundary, delete disposable backfill/runbook artifacts before commit, and regenerate current-file review bundles. Use `references/task-cleanup-and-stale-doc-reconciliation.md` when cleaning completed task directories, committing only cleanup deletions amid unrelated dirty work, or reconciling a task the user remembers as complete against stale TODO/progress docs. Use `references/out-of-scope-issue-cleanup-audit.md` when auditing `tasks/out-of-scope-issues/**/*.md` for obsolete/resolved issue files without deleting yet; it covers older issue-file formats, dirty worktree caveats, superseded baseline failures, and external/manual evidence standards. Use `references/buffdemy2-web-e2e-plan-doc-completion-refresh.md` when refreshing an existing Buffdemy2-web task plan after focused verification has landed but E2E matrix rows or closure gates remain, especially when fixture roles, named-user ACL paths, skipped Playwright tests, approved blockers, and final review artifacts need precise handling. Use `references/plan-code-final-doc-reconciliation-and-review-retry.md` when a completed `plan-code` task is resumed for “remaining tasks” after final verification/review, especially to audit stale live-doc wording, retry a previously blocked mandatory review leg, and avoid self-referential artifact-review loops. Use `references/plan-code-fresh-contract-e2e-proxy-cleanup.md` when a plan-code task removes/renames a canonical field and late review finds stale E2E selectors, wizard helpers, or test-only proxies still using the old contract; patch those surfaces, rerun touched Playwright/unit/build checks, and rerun final review because the approval is stale. Use `references/plan-code-stale-todo-finalization.md` when implementation, verification, and review artifacts already exist but live TODO/final-report docs are stale: verify saved review artifacts, rerun proportional current-tree checks, patch docs narrowly, and use a final artifact-consistency placeholder/exclusion to avoid stale-review loops. Use `references/plan-code-resume-after-task-artifact-removal.md` when a `plan-code` continuation finds the original task directory missing because later commits cleaned it up: verify current HEAD/status, avoid recreating removed artifacts, run remaining gates against the live branch, and log non-invasively. Use `references/plan-code-final-artifact-consistency-placeholder.md` when implementation reviews have passed but final review artifacts/TODO checkboxes still need to be written: create a placeholder consistency artifact, put task docs in final state, run a read-only artifact-consistency review excluding the placeholder itself, then overwrite it with the verdict. Use `references/plan-code-final-consistency-verdict-persistence.md` when the final consistency review has returned and you must save/canonicalize the verdict, reconcile stale session task-list state, or explain that the real remaining blocker is a manual/product gate rather than artifact consistency. Use `references/plan-code-post-verdict-completion-edits.md` when a consistency verdict passes but task/progress docs or canonical verdict artifacts still need completion edits; those edits require a narrow self-excluded artifact-consistency rerun before final completion is claimed. Use `references/plan-code-artifact-consistency-active-scope.md` when final consistency reviews are failing on stale historical bundles/raw transcripts, timestamp ordering, unchecked spec acceptance criteria, or superseded pending-review artifacts: judge active task docs/verdicts, treat historical implementation bundles as reference-only, and rerun after every live artifact edit. Use `references/plan-code-artifact-consistency-fallback.md` when that final artifact-only consistency gate needs a bounded fallback: if an async delegate consistency review does not start/complete/re-enter, recover it from logs/session history or use an interactive `claude-i` read-only consistency review, then overwrite the placeholder rather than leaving `passed: null`. Use `references/plan-code-manual-gate-finalization-and-compact-consistency.md` when human/manual Stripe/browser gates are confirmed late in a `plan-code` task: guide one action at a time, save non-secret manual-gate evidence, patch stale present-tense caveats in active evidence artifacts, and use a compact consistency bundle if broad artifact bundles time out. Use `references/plan-code-final-artifact-consistency-stale-doc-fixes.md` when that consistency review flags stale planning prose, broken historical task links, or old pseudocode after implementation review has already passed: patch the docs narrowly, rerun artifact-consistency, then overwrite the placeholder. Use `references/plan-code-current-only-rerun-bundles.md` when mandatory implementation review fails on stale/contradictory artifacts: save the failed verdict, mark historical probes superseded, regenerate a current-only bundle instead of embedding stale old bundles, and rerun review before final artifact consistency. Use `references/plan-code-final-bundle-sequencing.md` when final task-doc/report reconciliation, review bundles, raw reviewer outputs, aggregate `final-review.json`, and artifact-consistency checks interact; it covers stale embedded TODO snapshots and the rule that the reviewed bundle must not be regenerated after raw reviewer approvals unless both review legs rerun. Use `references/plan-code-required-field-db-update-and-extra-test-deviation.md` when a plan-code task adds a new required canonical field, existing dev DB rows need a no-compat one-off reset/update, and an extra non-required touched-fixture test is blocked by a pre-existing harness/environment issue. Use `references/plan-code-interactive-review-reconciliation.md` when a completed plan-code task discovers the required Claude Code review was run through print mode instead of interactive `claude-i`, or when stale embedded review bundles/checklists cause final artifact-consistency failures. Use `references/plan-doc-nonblocking-footgun-rerun.md` when a required plan-doc review passes but concrete non-blocking suggestions identify implementation footguns worth incorporating: save initial reviews as superseded, patch only plan docs, regenerate the bundle excluding review artifacts, rerun all required review legs, and save a final aggregate plus artifact-consistency check. Use `references/plan-doc-review-hardening-and-pending-codex.md` for the common explicit plan-doc variant where Claude/CLI review passes with useful hardening suggestions, task docs are patched, the old Codex-style delegate/bundle becomes stale, and the current Codex delegate is still pending: save superseded and pending artifacts, mark the aggregate blocked, and do not claim the full review gate is complete. Use `references/plan-doc-review-recovery-and-active-artifact-consistency.md` when a plan-doc review leg finishes but does not surface in the parent chat, a bundle's embedded scan/read evidence needs validation, terminal redaction may mask runnable env values, or final consistency checks must distinguish active artifacts from historical/superseded review evidence. Use `references/plan-doc-async-superseded-review-drain.md` when old async plan-doc delegate reviews return after newer bundle versions exist: save them as superseded, disposition findings against the current docs, update the pending aggregate history, and never count stale approvals as current-bundle approval. Use `references/plan-doc-stale-review-cache-adjudication.md` when cache/log recovery surfaces out-of-order delegate summaries or stale failures that quote old bundle wording: compare chronology/content, save resolved findings as `SUPERSEDED_FAILED_ADJUDICATED`, avoid unnecessary doc churn, and keep the current mandatory delegate pending until a current-bundle verdict is saved. Use `references/plan-doc-superseded-review-valid-blockers.md` when a stale plan-doc review contains blockers that still apply to current docs/source: save the stale verdict, patch only valid current blockers, mark all pre-patch review lanes superseded, regenerate the canonical bundle, rerun both lanes, and record true polish without entering an infinite stale-review loop. Use `references/identifier-field-to-autosuggest-plan-doc.md` when a plan-doc replaces raw ID/text fields with autosuggest or multi-select UI: explicitly gate both new-suggestion search and existing-ID hydration, preserve legacy values, define unresolved/over-max behavior, and test payload boundaries. Use `references/generic-selector-search-and-identity-apis.md` when designing reusable backend APIs for selector/autocomplete views: prefer existing username-only resource search or generic bounded search plus source-of-truth identity hydration over one tailored suggestion endpoint per frontend view. Use `references/plan-doc-current-user-owned-content-lookup.md` when a plan-doc must load/edit the current user's existing child content under a parent resource and the product contract is one item per user/parent; require reliable session-derived lookup/uniqueness gates and reject capped public-list filtering as the shipping lookup. Use `references/plan-code-review-driven-data-integrity-loops.md` when a large backend `plan-code` task passes builds/tests but independent review keeps finding live-path data-integrity defects in outbox propagation, reconciliation, indexes, or transactional side effects; fix the smallest blocker, search for stale patterns, regenerate the bundle, and rerun focused review after every source change. For Stripe/PaymentIntent-specific money-state loops where fresh create, stale reconciliation, and webhook paths must classify `succeeded` / terminal failure / nonterminal statuses consistently, use `references/plan-code-stripe-payment-intent-outcome-reruns.md`. Use `references/plan-code-stripe-webhook-real-card-gates.md` when Buffdemy plan-code work reaches Stripe real-card browser/E2E gates: verify live process env without reading forbidden `.env` files, distinguish endpoint reachability from signing-secret correctness, isolate declined-card evidence if accepted-card aborts the suite, and classify `stripe_webhook_signature_invalid` as an exact endpoint/listener secret mismatch investigation rather than a generic frontend/auth blocker. Use `references/plan-code-review-timeout-shard-recovery.md` when mandatory Codex-style delegate reviews time out on broad/sharded bundles: save timeout artifacts, fix any concrete mixed-shard blocker with RED/GREEN coverage, correct stale completion docs, regenerate the bundle, and retry with bounded micro-shards. Use `references/plan-code-review-blocker-rerun.md` when any mandatory `plan-code` review leg fails after another leg has already approved: save the failing verdict as superseded evidence, mark companion approvals stale after fixing source/tests, regenerate the bundle, rerun every required review leg, and reconcile task docs/final report before aggregate final review. Use `references/plan-code-phase-review-blocker-fix-rerun.md` for the follow-up pattern where a stale/superseded review returns valid blockers: adjudicate against the live tree, fix with RED regressions, rerun verification + simplify, regenerate the final bundle, save blocked reviewer artifacts when Claude Code is not logged in, and leave the phase incomplete until all mandatory lanes pass. Use `references/plan-code-opus-review-limit-and-rerun.md` when the mandatory Claude Code lane is explicitly overridden to Opus 4.8 @ xhigh effort and the run hits usage limits, stalls on a large bundle, returns `CHANGES_REQUIRED`, or passes while the companion Codex-style rerun remains pending; it covers blocked artifacts, interrupt/no-tools verdict recovery, stale delegate handling, and rerun sequencing. Use `references/plan-code-cross-repo-frontend-payment-review-rerun.md` for the frontend-payment variant where Stripe/PaymentElement code lives in a companion frontend repo, backend task docs hold the artifacts, review blockers require RED/GREEN catalog/schema/UI-contract fixes, and one review lane may pass while the Codex-style delegate remains pending. Use `references/plan-code-cross-repo-payment-review-blocker-recovery.md` when a later mandatory review fails after companion approval: save/adjudicate the failing verdict, RED/GREEN only valid blockers, regenerate the backend-held bundle, and rerun every mandatory lane. Use `references/plan-code-stripe-elements-lifecycle-finalization.md` when Stripe Elements/PaymentElement dialogs or editor wizards keep surfacing stale async lifecycle blockers near final review: store lifecycle tokens on payment sessions, test create/confirm/poll stale continuations, then finalize with pending-verdict retirement plus self-excluded artifact consistency. Use `references/plan-code-secret-safe-stripe-readiness.md` when remaining `plan-code` Stripe/manual gates require checking live env readiness without reading real `.env` / `.env.*` files: inspect running process/container envs with redacted shape checks, compare API/worker secrets by hash equality, prove webhook route reachability with safe unsigned rejection, and ask for small confirmations where frontend/Dashboard state is not observable. Use `references/plan-code-stripe-connect-manual-gate-guidance.md` when guiding final Stripe Connect/manual browser gates one instruction at a time: current Stripe Dashboard may expose Express through `Settings -> Connect -> Express Dashboard` rather than a separate “enable Express” toggle, and answer-tip earnings must be distinguished from cron-driven question-pledge settlement. Use `references/plan-code-manual-stripe-gate-finalization.md` when automated Stripe E2E is green and only manual Stripe/Connect/browser gates remain: guide one confirmation at a time, avoid preserving one-time Stripe URLs/secrets, prove answer-tip earnings and question-pledge settlement from live state, and rerun final artifact consistency after writing manual-gate evidence. Use `references/plan-code-modal-async-submit-lifecycle.md` for modal/dialog/wizard/editor mutation flows where close/open or external confirmation timing can reset or bypass duplicate-submit guards; it captures close-while-pending and confirm-before-saving regression patterns plus review checklists. Use `references/superseded-review-finding-recovery.md` when an async review result returns after its handle/bundle was superseded: inspect any concrete current-risk finding, save stale failures as superseded artifacts, fix valid blockers, rerun impacted verification, stale old pending artifacts, regenerate the final bundle, and dispatch a fresh reviewer. For the stricter mandatory-review variant where out-of-order delegate completions must be labelled by authority, saved as `SUPERSEDED_*` artifacts, adjudicated against the live tree, and used to stale every older pending review before rerunning all lanes, use `references/superseded-async-review-adjudication.md`. Use `references/plan-code-pending-delegate-review-blocker.md` when implementation/tests/build and one reviewer pass but a mandatory Codex-style `delegate_task` review never returns or cannot be recovered: save a blocker or pending artifact, update TODO/notes truthfully, and stop fail-closed instead of claiming plan-code completion. If the companion Claude/CLI leg passed, save that raw/structured approval separately, but do not create the aggregate `final-review.json` until the Codex-style verdict is saved or explicitly waived. A good pending artifact names the delegation id, reviewed bundle path, completed reviewer artifacts, current verification evidence, and exact resume steps; the live `todo.md` should remain `[~]` for the review gate and the final response should say blocked/pending, not complete. If the user later asks whether that review is complete, re-check the review directory, pending artifact, logs, and session history for the exact delegation id; no saved parseable verdict means the review is incomplete and should be rerun or explicitly waived. Use `references/plan-code-approved-companion-review-with-pending-codex.md` for the narrower case where the companion reviewer already approved and returned only non-blocking cleanup/testing-gap suggestions while the Codex-style delegate is still pending: record suggestions instead of optional churn that would stale the bundle, save a pending Codex artifact, leave aggregate review absent, and report implementation verified but review-blocked. Use `references/plan-code-final-bundle-after-status-stabilization.md` when final verification/status checks keep surfacing additional touched files after a final-review bundle was dispatched: verify the newly surfaced files, update docs/evidence, regenerate the bundle, supersede stale pending artifacts, and dispatch a fresh reviewer against the current tree. Use `references/plan-code-context-compaction-finalization.md` when a `/plan-code` session resumes after context compaction, delayed background-process notifications, or preserved task-list replay and the live working tree may already contain implementation, verification, or review artifacts: reconcile state from files/git first, chronologically supersede stale failure notices with newer green reruns, include untracked intended files explicitly in review bundles, keep async delegate review pending until a verdict is saved, and regenerate/re-review bundles after final task-doc/test/verification artifact edits. Use `references/plan-code-review-bundle-pending-state-ordering.md` when regenerating implementation review bundles around pending delegate state: patch live TODO/pending artifacts before bundle generation, avoid dispatching a reviewer and then editing artifacts it should have reviewed, and neutralize stale reviewer/tool names in generated bundles after a reviewer-model override. Use `references/plan-code-doc-only-review-failure-rerun.md` when a mandatory plan-code reviewer fails only stale/contradictory task-doc status: save the failing verdict, patch active docs narrowly, build a docs-rerun bundle with stale-phrase scans, and rerun the review before final consistency. Use `references/plan-code-unrecoverable-delegate-verdict-replacement.md` when a mandatory delegate review appears to finish internally but its final JSON verdict is not durably recoverable: save the lost delegate as superseded/unrecovered evidence, dispatch a fresh replacement against the current bundle, and update task docs so only the replacement is the active blocker. Use `references/plan-code-late-async-delegate-recovery.md` when the original async completion later surfaces with a parseable verdict after a replacement was dispatched: verify it covers the current bundle, save it as the recovered authoritative verdict if valid, supersede the replacement cleanly, and rerun final artifact consistency after doc/artifact updates. Use `references/buffdemy2-web-post-final-e2e-automation.md` when a Buffdemy2-web `plan-code` task has already reached final review/artifact-consistency and the user asks to automate a previously documented manual/browser smoke: run a RED stale-E2E check, update the nearest existing Playwright spec plus adjacent unit selector tests, rerun focused Playwright/Vitest/i18n/lint/build, then regenerate bundles and rerun mandatory review/artifact-consistency because prior approvals are stale. Use `references/plan-code-final-bundle-scope-and-scan-precision.md` when a final implementation bundle has many touched files or negative compatibility/fallback scans: mechanically validate that every modified tracked file appears in the bundle diff and snapshots, avoid path typos that create `[MISSING]` snapshots, and scope old-compatibility scans precisely so unrelated canonical helpers do not look like stale migration code. Use `references/product-decision-blockers-plan-code.md` when a plan-code blocker is really a product-contract decision: explain the contract gap, ask the smallest decision question, patch task docs immediately after the user answers, then resume tests-first and keep any remaining review-pending state explicit. Use `references/plan-code-endpoint-alignment-and-orphaned-stacks.md` when a frontend model/schema contract and backend verification use similar-but-different resource names or paths: trace frontend path modules to backend routes, classify live vs adjacent vs exported-orphaned stacks, document residual required-field risks, and avoid claiming that one route proves a different endpoint. Use `references/plan-code-legacy-vs-live-contract-evidence.md` when a `plan-code` contract task touches a legacy/exported model or route while current live UI/backend source uses a newer sibling route/model: distinguish legacy alignment from live-path proof, verify caller reachability, and patch task docs/bundles if review notes show endpoint evidence mismatch. Use `references/plan-doc-pending-delegate-review-blocker.md` when an explicit `/skill plan-doc` flow has task docs and a review bundle but the mandatory Codex-style `delegate_task` plan review does not surface: save a `plan-review-blocked.json`-style artifact, record resume steps, and do not claim the aggregate plan review is complete. Use `references/frontend-only-plan-doc-with-backend-companion.md` when a frontend `plan-doc` depends on a backend contract being implemented in a separate session: keep backend edits out of scope, write an explicit backend dependency gate, derive navigation/URLs only from authoritative response fields, split mocked frontend proof from backend-gated live/E2E proof, and save pending review artifacts rather than overclaiming if one review leg is still outstanding. Use `references/frontend-shared-component-plan-doc-surface-audit.md` when a frontend plan-doc changes layout/order/visual hierarchy of a reusable component: enumerate every current consumer (detail, feed preview, card, modal), make an explicit all-surfaces vs variant decision, add tests for each affected surface, broaden verification commands to include those test files, and rerun reviews after adopting even non-blocking doc hardening. Use `references/buffdemy2-web-shared-editor-upload-context-plan-doc.md` when planning Buffdemy2-web media upload changes through shared ArticleForm paper-editor components: scope Article-only context at the true `ArticleForm` boundary, keep shared `PaperEditor`/`ItemEditor`/`useOnFiles` neutral by default, test non-Article wrappers for no `postType` leakage, and block honestly if backend rejects existing non-Article `/media` flows. Use `references/cross-repo-plan-doc-companion-reconciliation.md` when a plan-doc in one repo discovers an existing companion task directory or dirty implementation work in another repo for the same contract: reference and reconcile the companion task, do not overwrite/absorb it, include both repos in the review bundle, and keep mandatory review-pending state explicit. Use `references/frontend-plan-code-backend-gated-partial-proof.md` for the matching frontend-only `plan-code` execution pattern: re-verify the backend gate, classify full-blocker vs partial live-proof blocker, continue only with deterministic frontend proof when allowed, correct stale Playwright project commands with `--list`, and do not claim backend-gated live E2E until the companion backend path lands. Use `references/backend-to-frontend-contract-handoff.md` when a completed backend plan-code task needs a durable handoff for a frontend planning session: summarize the implemented API contract, runtime error codes, UX implications, backend evidence, and live-backend alignment gates without over-prescribing frontend edits.

## When to Use

- The user wants a plan instead of execution.
- The user asks to create `tasks/<task-name>/` planning documents.
- The user asks to execute an existing plan phase-by-phase.
- The user asks to convert out-of-scope issue logs into task plans.
- The user asks to clean, classify, or delete completed task directories.
- The user asks for a high-quality implementation plan for another agent or developer.

## Explicit Slash-Flow Contracts

When the user explicitly invokes `/skill plan-doc`, `/plan-doc`, `/skill plan-code`, `/plan-code`, or says to use `plan-doc` / `plan-code`, treat that as a workflow contract, not a suggestion. These flows are fail-closed: if any required artifact or gate cannot be completed, stop and report the blocker instead of continuing as ordinary implementation.

### First Response Checklist

Before any implementation edit in an explicit slash-flow turn, state the selected mode and immediately perform the required first action:

- `plan-doc`: identify/create the task directory, then create or update task docs.
- `plan-code`: read the existing task docs first, then execute the documented TODOs.

Do not substitute a normal implementation plan, memory of the workflow, or an after-the-fact summary for these required first actions.

### Default Parallel Review Stack

Unless the user explicitly overrides the review stack, explicit `plan-doc` and `plan-code` workflows use a two-reviewer gate:

1. **Codex review** — run the normal Codex-style independent review from `requesting-code-review` against a saved, self-contained bundle.
2. **Claude Code Opus 4.8 @ xhigh effort review** — run Claude Code via the `claude-i` workflow in interactive mode, not `claude -p`. `claude-i` is a skill/workflow name, not necessarily a shell executable; for availability checks, `tmux` plus the `claude` CLI and a loaded/known `claude-i` workflow satisfy the requirement. Before sending the substantive prompt, verify the Claude Code TUI banner/status shows Opus 4.8 @ xhigh effort and save both raw and structured verdict artifacts. If Opus 4.8 @ xhigh effort or the requested effort cannot be selected, fail closed or record an explicit user-approved substitution; never substitute silently.

Run both reviews in parallel when safe: build one immutable review bundle first, launch the Claude Code Opus 4.8 @ xhigh effort review through `claude-i`, and dispatch the Codex-style review against that same bundle before adjudicating either result. Save separate raw reviewer artifacts plus one aggregate verdict artifact that records both verdicts, bundle path, reviewer model/tool/effort, static-scan status, verification status, and timestamp. Blocking findings from either reviewer fail the gate; non-blocking suggestions may be recorded without churn.

For `plan-doc`, review reruns are normally full dual-lane because the task docs are the product and are small enough to re-review. For `plan-code`, the first round of each gate is full dual-lane; after a clean baseline exists, post-fix reruns may follow `references/plan-code-delta-review-rerounds.md` so unchanged, unaffected files carry recorded clean verdicts forward while changed, semantically affected, and no-baseline files are reviewed. If the impact boundary is uncertain, rerun the full gate.

If the user waives or skips one review leg mid-session (for example, "skip Claude Code review for this task/session" after repeated stalls), record that as an explicit workflow override and do not invoke that reviewer again unless the user later explicitly asks for it. A later direct request such as "run Claude Code review on the diffs" supersedes the skip, but keep the rerun narrowly scoped to what was requested instead of restarting the full default gate.

For `plan-doc`, the bundle is the task docs, relevant current-code context, package/test-script context, git status, and any untracked task files. For `plan-code`, the full bundle is the implementation diff plus relevant untracked files, task docs, verification evidence, and static-scan results; a delta rerun bundle must also include prior clean baseline ids/artifact paths, changed files, semantically affected unchanged files, no-baseline files, carried-forward files, and the delta-interactions summary.

### `plan-doc` Required Sequence

For explicit `plan-doc` requests:

1. Stop before code changes unless the user explicitly combines planning and implementation.
2. Create or update `tasks/<task-name>/spec.md`.
3. Create or update `tasks/<task-name>/todo.md` or phase TODO files.
4. Include goal, scope/out-of-scope, acceptance criteria, decisions/open gates, implementation phases, verification commands, simplify/review gates, and manual-handling notes.
5. Include a copy-pasteable kickoff prompt for the implementer. The kickoff prompt must mention the task directory, objective, ordered phases, the default parallel Codex + Claude Code Opus 4.8 @ xhigh effort review stack, verification commands, out-of-scope constraints, and final-report expectations.
6. Run the default parallel review stack on the plan-doc bundle unless the user explicitly overrides review. Save the Codex review artifact, the Claude Code Opus 4.8 @ xhigh effort (`claude-i`) artifact, and an aggregate plan-review verdict under `tasks/<task-name>/reviews/`. If the Codex-style delegate review completes in logs but the verdict does not re-enter the parent chat, use the `requesting-code-review` reference `references/delegate-review-status-recovery.md`: recover the subagent session via logs/session_search, save the parseable verdict artifact, and record the recovery path in the aggregate instead of rerunning or treating the dispatch alone as approval.
7. If reviewer suggestions change the plan docs, regenerate the bundle and rerun both review legs before presenting the plan as reviewed.
8. Save each raw review artifact immediately after that review leg completes. If saving the artifact fails or tools become unstable, fail closed: stop before launching any remaining review leg, report the exact artifact path that could not be saved, preserve the reviewer verdict in the main response, and instruct the user how to resume. If the legs were launched in parallel before the save failure surfaced, do not adjudicate or aggregate the gate; cancel, discount, or mark any in-flight companion verdict as incomplete/stale before stopping. Do not proceed to a new Claude Code review or aggregate verdict creation when the preceding Codex-style artifact could not be durably written.
9. Present the docs/kickoff to the user and stop unless implementation was explicitly authorized.
10. If a required step is skipped, say exactly which step was skipped and why; do not imply the flow was completed.

### `plan-code` Required Sequence

For explicit `plan-code` requests:

0. Treat `plan-code` as an implementation workflow. If the user mentions a "final review", "post-implementation review", Claude review, or review artifact in the same request, that review is the final gate **after** implementation unless the current tree already contains a completed implementation and updated task progress. Do not run only the review against untouched plan docs and call the task done; either start coding or explicitly report that no implementation exists and ask whether to implement now.
1. Read `tasks/<task-name>/spec.md` and `todo.md` before editing implementation files.
2. If task docs are missing or lack acceptance criteria / review gates / verification, create or update them before coding.
3. Execute the TODOs phase-by-phase and update progress as work completes. If a required review returns blocking findings during the same active `plan-code` flow, continue fixing those blockers on your own, rerun impacted verification, update the clean-baseline ledger/scope notes, and rerun required reviews using a full or delta bundle per `references/plan-code-delta-review-rerounds.md`. Do not stop to ask for permission to fix review blockers unless the fix requires a product decision, broad scope change, destructive operation, or explicit user choice.
4. Run the simplify gate after implementation and after review/build fixes; for reruns, scope simplify to changed and semantically affected files unless the impact boundary is uncertain. Record any intentional non-simplification when broad refactors would increase risk.
5. Run the default parallel review stack before completion: Codex-style review via `requesting-code-review` plus Claude Code Opus 4.8 @ xhigh effort review via `claude-i`, both against the same saved full implementation bundle whenever safe. Save the full-round clean baseline before carrying any verdict forward.
6. Save the final aggregate review verdict under `tasks/<task-name>/reviews/final-review.json` or document why an equivalent artifact path was used. The aggregate verdict must reference the Codex review artifact and the Claude Code Opus 4.8 @ xhigh effort (`claude-i`) artifact, record the model/effort actually shown by Claude Code, and name each review round as full or delta; the task is not complete unless both required legs pass or the user explicitly waives one.
7. If any code, test, fixture, migration, task doc, or intended commit artifact changes after review, the prior approval is stale for the changed/affected scope. Rerun impacted verification, classify the fix with `references/plan-code-delta-review-rerounds.md`, and rerun the review gate as full or delta according to that classification. If the reviewer-driven fix changes scope or reveals a missing handoff step, update the task spec/TODO before the rerun so the final artifact describes what was actually implemented. Before the final review, either put TODO/progress docs into their intended final state or plan one last artifact-consistency review after changing them; do not make unchecked final TODO edits after the final approval and then claim the review covered the final bundle. When finalizing docs after async reviewers or compaction, re-read the live TODO/progress snippets immediately before patching, because sibling agents or resume-time updates may have changed wording; patch the current active block narrowly, then search for stale `pending`/`incomplete` phrases and convert historical checkpoints to past tense so they do not contradict the final completion entry.
8. If review artifacts themselves are saved after the review verdict (for example Claude pane captures, final-review JSON, or consistency-review JSON), avoid infinite stale-artifact loops by using one of these patterns:
   - create a pending placeholder artifact before the final consistency review and overwrite it with the verdict afterward; or
   - run a final read-only artifact-consistency review scoped only to task docs/review artifacts after writing the verdict.
   Do not leave `passed: null` / pending placeholders behind in final artifacts.
9. Run the verification commands from the task docs with real tool output.
10. If the user later asks to commit/push a completed `plan-code` task, first reconcile the task progress docs with the actual completed work: update/check off completed TODO items, confirm no unchecked/in-progress/blocked items remain for the target task (ignore status-legend examples, but not real checklist rows), and stage only that task's implementation/docs/review artifacts. If a remaining `[!]` item is a genuine completed verification deviation rather than pending work (for example authenticated browser smoke was environment-blocked but covered by tests and review), convert it to a checked item that explicitly documents the deviation before staging. If an unchecked real TODO contradicts the current source (for example a required reconciliation source, transaction invariant, or test matrix row was not actually implemented), do not paper it over as a doc cleanup: return to implementation, add/adjust focused tests, rerun impacted verification, regenerate the bundle, and rerun final review before staging. If the TODO/final-report claims a specific reviewer/tool, broad test command, or artifact path, verify the saved artifact actually substantiates that claim; otherwise rewrite the doc to the truthful fallback or mark the command N/A before final consistency review. Any TODO/review-artifact edit made for commit readiness is a post-review artifact change: rerun a final artifact-consistency review on the exact pre-commit bundle before committing. Leave unrelated incomplete `tasks/` directories uncommitted unless the user explicitly includes them. When task directories are untracked/stale or unrelated task dirs are present, follow `references/plan-code-commit-readiness-untracked-tasks.md`: avoid `git add -A`, stage intended implementation paths explicitly, and report whether task removal was an untracked cleanup rather than a tracked deletion.
11. If `plan-code` legitimately stopped at an early blocker gate before implementation (for example a fail-closed data-contract gate), do not pretend the normal simplify/review/final-review sequence ran. If the user asks to commit/push that blocked outcome, commit only the blocker documentation/artifacts that were produced, run lightweight artifact checks such as scoped `git diff --check`, and report the commit as a blocker artifact rather than a completed implementation. Do not create a fake passing final-review artifact for code that was never implemented.
12. If the user explicitly asks for a final review while the task is still docs-only/unimplemented, run the requested read-only review gate against a bundle that proves the current state, save a failing `CHANGES_REQUIRED` artifact if the reviewer returns one, and report the task as blocked/not complete. A failing final-review artifact is allowed as durable evidence of the gate; it does not satisfy completion or replace the missing implementation/simplify/verification steps.
13. Final response must include completed phases, verification commands/results, review artifact path and verdict, remaining risks, and any deviations.

### Mandatory Final Self-Audit

Before finalizing an explicit `plan-doc` or `plan-code` flow, perform and report a compact audit:

- Task docs created/updated: yes/no
- TODO/progress updated: yes/no
- Kickoff prompt provided or used: yes/no/not applicable
- Simplify gate run: yes/no/not applicable
- Codex review run: yes/no/not applicable
- Claude Code Opus 4.8 @ xhigh effort (`claude-i`) review run: yes/no/not applicable
- Review baselines and delta/full rerun scopes recorded: yes/no/not applicable
- Parallel review aggregate artifact saved: yes/no/not applicable
- Post-review edits made: yes/no
- Review rerun after post-review edits: yes/no/not applicable
- Verification passed with real output: yes/no/not applicable
- Deviations documented: yes/no/not applicable

Any required `no` blocks claiming completion.

## Choose the Mode

### Plan-only mode

Use when the current turn must produce a markdown plan and avoid implementation. Inspect context read-only, write the plan under `.hermes/plans/` when appropriate, and do not mutate project code.

### Write implementation plans

Write for an implementer with little context: exact files, current behavior, target behavior, step-by-step tasks, tests, verification, risks, and rollback/cleanup notes. Favor bite-sized tasks and explicit acceptance criteria.

### Task document creation

For Claude/Hermes `plan-doc` style work, create `tasks/<task-name>/spec.md` plus `todo.md` or phase TODOs. Include dependency/parallelization analysis, decision gates, manual-handling notes, simplify/review gates, and verification. When the plan is an API/public-contract rename, use `references/api-rename-plan-pattern.md` for the compatibility-alias/canonical-response pattern.

When the plan-doc is still in discussion mode and the user is choosing between flows, keep replies short and decision-oriented: state the current decision point, the recommended default if useful, and then wait. Do not send long descriptive recaps while the user is reading or deciding. After the user chooses, patch the task docs directly and summarize only the changed plan semantics plus files touched.

If the user overrides normal review gates (for example, “skip Claude-i review but keep Codex peer review”), preserve that exactly in the plan’s review-gate section and final-report checklist instead of applying the default review stack mechanically.

When updating an existing plan after a user reports a quality problem, keep the spec at the policy/acceptance-criteria level unless the user explicitly approves an implementation strategy. Do not turn an observed regression into tailored fixture logic or prescribe a dictionary/heuristic overlay just because the current code happens to be heuristic; capture the observed data, the acceptance criteria, and a decision gate to evaluate/replace the provider instead.

When updating an existing plan-doc from a bug report or behavior complaint, first ground the spec change in read-only evidence where practical: inspect current task docs, relevant implementation/tests, and local/dev data if the user explicitly asks to compare real data (for example stored DB fields vs freshly computed results). Record durable findings in the task's `notes.md`, convert them into concrete `spec.md` decisions, and add executable TODO/verification items so `/plan-code` cannot skip the newly discovered edge case. If a clarification times out or is not provided, choose the safest interpretation that preserves the user's stated goal and document that choice as a decision rather than leaving the plan ambiguous.

When the user asks for a new clean task after a previous task direction was blocked, superseded, or based on a wrong assumption, create a fresh `tasks/<new-slug>/` file set instead of mutating the old task. In the new `spec.md`, state which old task it supersedes and what contract changed; leave the old task intact unless the user explicitly asks to remove it. If the user later asks whether the old directory can be removed, inspect tracked/untracked status and unique content first, then recommend a tracked deletion (`git rm -r tasks/<old-slug>`) only when the new task captures the intended replacement.

For bug-report `plan-doc` work where the real repro flow is complex or user-operated (uploads, payments, auth-specific UI, cross-device steps), do not interrupt the docs-only task by asking the user to manually retry unless the plan itself cannot be written without it. Instead, encode a Phase 1 evidence gate and a copy-pasteable manual-test request in the TODO/kickoff prompt: capture the exact hidden error boundary first, ask the user for one focused manual run only if local automation/log inspection cannot expose it, and require the implementer to record the user's result before changing source code. Also check `git status --short` before creating task docs and explicitly avoid unrelated untracked task directories so plan-doc output does not absorb another agent/user's work.

When the user asks for an external/Claude review of the plan itself, run it as a read-only plan-doc review gate rather than treating it like implementation review. Bundle the current task docs explicitly (including untracked `tasks/<slug>/` files, git status, relevant package scripts, and a simple secret/static scan), ask the reviewer for a bounded verdict, incorporate useful non-blocking doc suggestions, then regenerate the bundle and rerun the review so the saved artifact is not stale. Validate the saved bundle before dispatch: if Hermes `read_file`/script helpers inserted cache/dedup placeholders (for example `dedup`, `content_returned: false`, or “refer to earlier read_file result”) instead of real file contents, or if truncation/omission markers appear, regenerate the bundle from direct filesystem reads before review. Save the final review under `tasks/<slug>/reviews/` (for example `claude-plan-doc-review.md`) and report whether any suggestions remain. For multi-repo plan-docs, include each repo's task docs/status and relevant source excerpts in the same immutable bundle, but keep implementation handoff docs in their owning repos when the implementers will work there; record the aggregate verdict in the origin task directory and name companion backend/frontend docs explicitly in the final report. If a first review produces blockers or useful non-blocking plan improvements, save that initial artifact as superseded/initial, patch the docs, regenerate the bundle, and rerun all required review legs before claiming the plan is reviewed. If a reviewer cites project docs or guide files as contract evidence, classify their authority before accepting the finding: for Buffdemy backend, `docs/` is context-only by default, so a docs-only blocker should be saved as superseded, the plan/bundle should be patched to state the authority rule, and both review legs should be rerun. If parallel review legs are running and one reviewer returns `CHANGES_REQUIRED` before the other finishes, capture the unfinished reviewer pane/output as `INCOMPLETE_STALE`, stop that session, patch the docs, regenerate the bundle, and rerun both review legs on the new bundle; never count the unfinished pre-fix review as approval. When regenerating plan-doc bundles, exclude the review artifacts themselves from the reviewed scope or clearly document the self-exclusion, then write a final artifact-consistency check after aggregate verdict creation to avoid self-referential stale-review loops. If the user later corrects an authoritative input used by the plan/review (for example the target backend repo or whether `docs/` is authoritative), immediately mark the old review artifact superseded/stale, redo the audit against the corrected source, and rerun the review before implementation. See `references/plan-doc-external-review-gate.md` for a concrete pattern.

### Plan execution

For `plan-code` style work, load the existing plan, decide serial vs parallel execution, implement safe independent batches, update progress, run simplify/review gates, verify builds/tests, and report completion with evidence. If the user invokes a slash-skill flow such as `/skill plan-doc` or later asks whether `plan-code` was followed, treat that as a workflow contract: explicitly load/consult the governing skill or umbrella, document any deviation, and run the missing gate rather than claiming the flow was satisfied by ordinary implementation.

When the user asks to "resume and complete" an existing task but explicitly says to first examine/report remaining tasks and not implement or test without approval, treat the first turn as a read-only resume audit. Read the task docs, current relevant source, and git status; distinguish stale unchecked TODOs from implemented code; identify unrelated dirty work; report remaining implementation/test/doc-reconciliation work; then stop for approval. Do not run tests, builds, browser smoke, or make task-doc checkbox edits during that audit even if they look obvious.

When a `plan-code` task spans a large backend/API surface and the final review produces late blockers after prior green verification, first use `references/plan-code-large-diff-budget-and-checkpoints.md`: checkpoint current passing evidence, split by contract-level micro-bundles, fix only blockers that can be regression-tested and re-reviewed within the remaining budget, and stop with a fail-closed handoff rather than starting broad unverified patches near the tool/time limit. If the blockers are live-path data-integrity mismatches across outbox deltas, cron reconciliation, unique indexes, or wallet/payment semantics, also use `references/plan-code-review-driven-data-integrity-loops.md`: align live update semantics with reconciliation semantics, search for exact stale patterns after each patch, regenerate the final bundle, and rerun a focused review after the last code change.

For Buffdemy backend Elasticsearch/search projection lifecycle execution, use `references/buffdemy-search-projection-plan-code-lifecycle.md`: reset/backfill is a real implementation gate; if a non-destructive reconciliation path is chosen, prove it converges stale docs (non-ready rows, empty-content parents, missing roots) and rerun final reviews after adding or fixing the command.

When a plan labels a step as manual, first distinguish **human-only decision/observation** from **agent-runnable verification**. If the manual item is just commands, unit tests, Docker tests, repo inspection, env/preflight checks, or other tool-executable evidence, run it yourself and record the output instead of asking the user to do it. Stop for the user only when the gate requires an external human action/decision or a manual UI/device observation that cannot be exercised with available tools. If a direct host command is unavailable, try the project-documented container/dev-stack path before handing it back to the user.

When a plan replaces a heuristic with a third-party provider or native dependency, treat deployment viability as part of execution, not a follow-up note: verify the dependency installs/loads in the target runtime, exercise the relevant dev/container path, and run the production/build path where practical before marking the phase complete. If deployment is not viable, stop and report the blocker rather than silently falling back to the old heuristic.

When a plan-code task adds a new required canonical field and no-compat/pre-service data reset is allowed, treat existing local/dev data as an execution gate: query the live dev DB through the configured app/container environment, run a one-off inline update/reset if needed, record before/after counts, and verify no disposable migration script remains. If you update an obvious typed fixture outside the documented focused test set and its own extra test is blocked by a pre-existing harness issue, do not broaden into test-harness repair unless required; rerun the documented verification plus typecheck/lint and record the extra-test blocker as a deviation. See `references/plan-code-required-field-db-update-and-extra-test-deviation.md`.

When a `plan-code` task spans multiple repos and local verification depends on a running service/container, verify the live service's source mount or deployment source before editing a different checkout or claiming live smoke coverage. If the task's planned backend path differs from the backend actually serving the frontend, document the mismatch in task notes and stop before modifying the live path unless the user explicitly approves switching repos/paths. See `references/cross-repo-live-backend-path-gate.md`.

For Buffdemy2-web Article/ArticleComment editor-route or owner-action work, use the authenticated Playwright save/restore smoke pattern in `references/buffdemy2-web-plan-code-editor-smoke.md`: stored auth states for owner/non-owner/anonymous checks, temporary marker edits restored before completion, and console scanning for runtime i18n misses.

For Buffdemy2-web browser/API smoke when the dev server is already running on the user's host, use `references/buffdemy2-web-host-smoke.md`: probe and target `host.docker.internal:3000` from Hermes/WSL. If saved Playwright auth cookies are scoped to `localhost`, refresh/regenerate storage state for `host.docker.internal` or configure an explicit bridge; do not silently switch host-run server checks to agent-local `localhost`.

For Buffdemy2-web E2E stabilization work, use `references/buffdemy2-web-e2e-stabilization.md`: target host-running services through `host.docker.internal`, make cleanup failures visible, inspect frontend/backend API schema contracts before changing locators, handle titleless backend tiny shapes (such as list-item `ArticleTinyData`) as intentional metadata-only references, scope modal locators, harden comment/mention helpers against stale toasts/caret drift, and record dev-server/browser-project blockers separately from real test failures. When Hermes must rerun E2E against a temporary local Next server or auth/storage state fails before product assertions, also use `references/buffdemy2-web-e2e-local-env-auth.md` for base-URL, Redis, callback, and Playwright auth-state alignment.

For Buffdemy notification summary key cleanup/refactors, use `references/buffdemy-notification-summary-mapping.md`: verify `/workspace/dev/buffdemy-backend` as the target backend, treat `/workspace/dev/buffdemy2-api` as legacy unless explicitly requested, prefer target-first public keys, and first confirm whether the product is pre-service. If notification data can be flushed, plan a canonical-only reset with no legacy aliases/dual-accept/migration; use staged compatibility only when production/backward compatibility is required.

When a `plan-code` request asks for a clean worktree from a base branch but the current checkout is dirty, an old worktree path is broken/stale, or the user explicitly waives actual test runs, use `references/clean-worktree-plan-code-salvage-and-test-waiver.md`: create a separate clean worktree, salvage only scoped task files if useful, regenerate review artifacts instead of reusing stale approvals, mark waived tests as deviations, and normalize generated review bundles before staging.

### Issue conversion

For `plan-issues` style work, scan `tasks/out-of-scope-issues`, filter by priority, group and deduplicate issues, resolve decisions up front, and invoke the task-doc workflow. Do not implement fixes during issue conversion.

Because issue conversion invokes task-doc creation, the generated task plans require the explicit `plan-doc` review gate unless the user explicitly waives it. A coverage map and `git diff --check` are useful but insufficient. If multiple grouped task dirs are generated, review them one by one by default: build a self-contained plan-doc bundle for the first task, run Codex-style delegate + Claude Code Opus 4.8 @ xhigh effort (`claude-i`), save raw and aggregate review artifacts, patch/rerun both legs after any reviewer-driven doc edits, and only then proceed to the next task. A dispatched delegate is not approval; save a pending/blocker artifact and report the flow as incomplete if the mandatory Codex-style review has not returned.

### Plan cleanup

For `plan-clean` style work, classify task directories as complete, incomplete, or ambiguous. Use dry-run first. Delete only confirmed paths inside `tasks/`, and only when all checkable plan items including verification are complete.

When the user asks to re-examine incomplete or ambiguous task directories, do a read-only **codebase audit**, not just a checkbox recount. Task docs can drift: stale unchecked TODOs may remain after a completed implementation, while all-checked tasks can still be incomplete if current code violates the spec or focused tests fail. Read the task docs, extract acceptance criteria, inspect the current code/tests (and referenced cross-repo surfaces when applicable), run proportional focused verification when safe, then classify as complete, complete-with-caveat, incomplete, or parked/deferred. Use `references/plan-clean-codebase-audit.md` for the detailed pattern and red flags.

If the audit finds that current source implements a newer/reset contract while old phase TODOs describe a superseded hybrid/migration path, use `references/stale-task-reset-reconciliation.md`: reconcile the task docs as completed/superseded/deferred instead of blindly coding obsolete checkboxes, run focused verification, and save fresh final review artifacts when the workflow requires them.

## Cross-Cutting Rules

- Planning output must be concrete enough for another capable agent to execute without rediscovery.
- For indexed repositories, use Codebase Memory MCP as the first source-discovery pass in plan-doc/plan-code work: check indexed projects/status, then prefer graph/code search and path tracing before direct grep-style scans. Use direct file tools for exact reads, untracked/generated files, or when the index is missing/stale; if skipped, say why. Avoid hardcoding machine-specific Codebase Memory project names in durable task docs or repo instructions because different machines can index the same repo under different names.
- When writing a copy-paste handoff prompt for a host-side developer/agent from inside the Hermes `/workspace` mount, translate paths to the recipient's filesystem view, but do not assume a single Buffdemy host convention. Ask once or follow the user's latest correction exactly (for example a user may want `/workspace/dev/...` rendered as `~/workspace/dev/...`, not `~/dev/...`; they may later reverse that for a host-side agent). Keep internal Hermes tool paths unchanged unless the active workspace tag says otherwise.
- For copy-paste handoff prompts, avoid nested Markdown code fences that can break rendering around path blocks. If the user reports broken Markdown or asks for plain text, regenerate the whole prompt as plain text with simple headings and un-fenced command/path lines.
- Do not hide unresolved decisions; mark them as gates.
- Preserve out-of-scope findings for later triage instead of silently dropping them.
- Execution claims require real test/build/tool output.
- For `/plan-code` verification inside disposable Docker containers, if known-good credentials or test URIs arrive inside the container as redaction placeholders, stop retrying inline `docker create -e` / `docker exec ... export` variants. Use the TDD skill's `references/docker-secret-safe-test-runners.md` pattern: copy in a tiny runner script that constructs the env var inside the container, then run the real package-local test/build command through it.
- For `/plan-code` focused RED/GREEN runs in stale disposable images, avoid chasing missing-module failures one file at a time. Copy the current app/package source subtree and package-local config into the container first, then rerun from the package directory so RED reflects product behavior. See the TDD skill's `references/disposable-container-monorepo-red-green.md`.
- Cleanup is conservative: ambiguity means keep or ask, not delete.
- When a user asks to remove a completed task directory after commit/push, first verify the latest commit contains the task artifacts and the branch is synced with upstream, run `git rm -r --dry-run -- tasks/<slug>`, confirm the dry-run lists only that directory, then apply `git rm -r -- tasks/<slug>`. Report the staged deletion separately from committing/pushing it.

## Mid-task Contract Changes

- When the user changes requirements during `/skill plan-code`, follow `references/mid-task-contract-changes.md`: test the new contract, remove stale compatibility paths, update plan artifacts, rerun verification, and regenerate/re-review final bundles.
- When the change is specifically “this is pre-service; no backward-compat code; DB/index can be reset,” follow `references/pre-service-no-compat-plan-code-cleanup.md`: tighten producer/message/consumer/model boundaries, remove old-shape fixture support, delete disposable rewrite/runbook artifacts before commit, and regenerate review bundles from current file contents so deleted compatibility prose is not preserved as live evidence.
- When a backend/API gate resolves with a simplified generic selector contract, refresh the blocked frontend plan before coding: write `backend-contract.md`, update spec/TODO/progress, mark stale handoff prompts/review bundles as historical, and clarify that “hydration” may simply mean resolving stored IDs through the normal public lookup while save payloads stay ID-only. See `references/generic-selector-search-and-identity-apis.md`.
- When task docs created by the agent contain an over-specific TODO that is not actually required by the user's source instructions and would broaden the public/API contract, do **not** implement the broader behavior just to satisfy the checkbox. Re-read the source instructions, keep the narrower requested contract, and patch the task docs before review to document the intentional deviation and why the generated TODO was not implemented.
- If file/terminal tools become inconsistent during a mandatory `/plan-code` review or finalization step (for example minimal commands are interrupted, file reads contradict earlier state, or the active checkout cannot be verified), fail closed: stop making edits/review claims, report the exact blocker, and resume only after tool access is stable. Do not fabricate review artifacts or mark gates complete from memory.
- When a final artifact-consistency review is used to validate completed task docs and review artifacts, avoid self-referential stale loops: either exclude the consistency artifact itself from the review scope and state that explicitly in the saved JSON, or create a placeholder before review and do not judge its timestamp/content as part of the same review. After rerunning reviews or writing final verdict artifacts, remove or clearly supersede stale duplicate review files so the task directory contains one canonical artifact per gate.

## Verification Checklist

- [ ] Mode was selected explicitly from the user's request.
- [ ] Plan/task docs include paths, commands, tests, and acceptance criteria.
- [ ] Execution updated progress and verified with real outputs.
- [ ] Cleanup used dry-run/classification before any deletion.
