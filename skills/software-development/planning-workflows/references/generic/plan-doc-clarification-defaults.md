# Plan-doc clarification defaults

If a blocking product/architecture decision is missing, ask a concise clarifying question. If the user asks you to decide or a timeout policy applies, choose the safest option that preserves the user's stated goal and record it in `spec.md` as:

```text
Decision: <choice> (Hermes default — no user input)
```

Avoid leaving plans ambiguous.
